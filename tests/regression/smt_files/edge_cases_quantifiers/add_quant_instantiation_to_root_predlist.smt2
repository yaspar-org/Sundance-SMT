(set-option :auto_config false)
(set-option :smt.mbqi false)
(set-option :smt.case_split 3)
(set-option :smt.qi.eager_threshold 100.0)
(set-option :smt.delay_units true)
(set-option :smt.arith.solver 2)
(set-option :smt.arith.nl false)
(set-option :pi.enabled false)
(set-option :rewriter.sort_disjunctions false)
(declare-sort % 0)
(declare-sort Poly 0)
(declare-fun B (Bool) Poly)
(declare-fun %B (Poly) Bool)
(declare-sort T 0)
(declare-const BOOL T)
(declare-sort D 0)
(declare-const $ D)
(declare-fun has_type (Poly) Bool)
(declare-const P Poly)
(declare-fun %%apply%%0 (% Poly) Poly)
(declare-const S %)
(declare-const S1 %)
(declare-const S2 %)
(declare-const s1! Poly)
(declare-const s2! Poly)
(declare-const s3! Poly)
(declare-const !!skolem_variable_4 Poly)
(declare-const a Poly)
(declare-const c Poly)
(assert (not (has_type a)))
(declare-const a$skolem Poly)
(assert (forall ((b Bool)) (! (has_type (B b)) :pattern ((B b)))))
(assert (forall ((x Poly)) (! (= x (B (%B x))) :pattern ((has_type x)))))

; note this is not necessary for unsat but was necessary to get sundance to screw up
(assert (= c (B true)))
(check-sat)
