(set-option :auto_config false)
(set-option :smt.mbqi false)
(set-option :smt.case_split 3)
(set-option :smt.qi.eager_threshold 100.0)
(set-option :smt.delay_units true)
(set-option :smt.arith.solver 2)
(set-option :smt.arith.nl false)
(set-option :pi.enabled false)
(set-option :rewriter.sort_disjunctions false)
(declare-const x Bool)
(declare-sort % 0)
(declare-sort P 0)
(declare-fun B (Bool) P)
(declare-fun %B (P) Bool)
(declare-sort T 0)
(declare-const O T)
(declare-sort D 0)
(declare-fun h (P) Bool)
(declare-fun % (% P) P)
(declare-fun v (P P) Bool)
(declare-fun s (P) P)
(declare-const & D)
(declare-const T T)
(declare-const p %)
(declare-const p4 %)
(declare-const a1 P)
(declare-const t P)
(declare-const s1 P)
(declare-const s2 P)
(declare-const r1 P)
(declare-const r2 P)
(declare-const b1 Bool)
(declare-const b2 Bool)
(declare-const b3 Bool)
(assert (h a1))
(declare-const a P)
(assert (not (= b1 b2)))
(assert (forall ((x P)) (! (= x (B (%B x))) :pattern ((h x)))))
(assert (= (%B r1) b1))
(assert (= b2 (%B r2)))
(assert (= b1 b3))
(assert (= a1 (B true))) ; z3 is unsat without this, but sundance becomes unsat with this
(assert (= r2 (B (or b1 b3))))
(assert (= (B x) r1))
(check-sat)
